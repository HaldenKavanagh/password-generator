when you click the generate button 
    prompt for how many characters you want
        must be between 8 and 128
    prompt for lowercase
    prompt for uppercase
    prompt for numbers
    prompt for charactors

need variables for ALL uppercase, lowercase, numbers, and special charactors inside the generatePassword function

calls for the selected criteria(if statemants)

Math.floor and Math.random functions to generate a random password form the users selected criteria.




